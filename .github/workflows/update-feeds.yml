name: Update RSS Feeds

on:
  schedule:
    - cron: '0 * * * *'  # Every hour (24 times/day)
  workflow_dispatch:  # Manual trigger

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        
    - name: Install dependencies
      run: pip install feedparser
      
    - name: Fetch RSS feeds
      env:
        NEWSDATA_API_KEY: ${{ secrets.NEWSDATA_API_KEY }}
      timeout-minutes: 5
      run: python scripts/fetch_rss_fixed.py
      
    - name: Fetch price data
      run: python scripts/fetch_prices.py
      
    - name: Generate missile defense stats
      run: python scripts/generate_missile_stats.py
      
    - name: Commit and push changes
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add public/incidents.json public/prices.json public/moi_missile_stats.json
        git diff --staged --quiet || (git commit -m "Update incidents, prices, and missile stats from RSS feeds" && git push)
